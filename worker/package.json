{
  "name": "image-host-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for GitHub image host",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler preview"
  }
}
